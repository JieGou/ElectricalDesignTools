<UserControl x:Class="WpfUI.Views.TestDteqView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="350"
             Background="LightCyan">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Tag}" FontSize="14" FontWeight="Bold"
                   d:Text="DTEQ TAG"/>
        
        <TextBlock Text="Power Factor: " Grid.Row="1"/>
        <TextBlock Text="{Binding PowerFactor}" Grid.Row="1" Grid.Column="2" FontWeight="Bold"/>
        <TextBlock Text="Cable Size: " Grid.Row="2"/>
        <TextBlock Text="{Binding Path=Cable.CableSize}" Grid.Row="2" Grid.Column="2" FontWeight="Bold"/>
    </Grid>
</UserControl>
