<UserControl x:Class="WpfUI.Views.ProjectSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:commands="clr-namespace:WpfUI.Commands"
        xmlns:local="clr-namespace:WpfUI.Views"
        mc:Ignorable="d" 
        d:DesignHeight="800" d:DesignWidth="1200" d:Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="btnSelectProject" Content="Select Project" 
                Grid.Column="1"
                Grid.Row="1"
                Height="27"
                Command="{Binding Path=SelectProjectCommand}"/>
        <ListBox x:Name="lstStringSettings" Grid.Column="2" 
                 Grid.Row="1"
                 Width="100" Height="100"/>

    </Grid>
</UserControl>
